在文档中提到，该实例创建完毕后，在浏览器地址栏中输入http://localhost:8080/MyServlet/textServlet地址来查看实例运行结果。
其中，地址中的MyServlet为本实例名称，也就是项目名称。
因为，在将本书各章实例的源程序放入资源包时，采用了“章号/实例号”的方式进行存放（如，06/01，表示第6章，例6.1），所以读者需要修改本实例的名称为MyServlet后，才能通过上面的地址进行访问，
读者也可以不修改实例名称，而将访问地址修改为http://localhost:8080/01/textServlet，同样可以运行实例。